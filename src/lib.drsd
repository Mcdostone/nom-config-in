use nom_locate::LocatedSpan;
use regex::Regex;
use std::path::PathBuf;
use std::{fs, io};

//pub mod attribute;
pub mod entry;
pub mod symbol;
pub mod util;

pub use self::entry::Entry;

pub type ConfigInput<'a> = LocatedSpan<&'a str, &'a str>;

#[cfg(test)]
pub mod lib_test;
#[cfg(test)]
pub mod symbol_test;
#[cfg(test)]
pub mod util_test;